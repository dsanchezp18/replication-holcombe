# code/python

## Purpose
A small side-analysis in Python (descriptive statistics, a correlation pairplot). The main replication is done in R; this is not a full translation.

## Contents
- `01_descriptive_analysis.py`

## Naming Convention
`NN_verb_description.py`.

## Dependencies
Requires `data/final/corruption_final.csv`. Run from the project root (paths are root-relative): `python code/python/01_descriptive_analysis.py`.
