# outputs

## Purpose
Everything generated by `code/`. Nothing here is edited by hand — regenerate instead of patching.

## Contents
- `graphs/` — figures (`.png`).
- `tables/` — descriptive-statistics and regression tables (`.xlsx`, `.tex`).

## Naming Convention
`fig_description.ext` for graphs, `description.ext` or `tab_description.ext` for tables.

## Dependencies
Produced by `code/analysis/02_figures.R` and `code/analysis/04_summary_tables_export.R`.
